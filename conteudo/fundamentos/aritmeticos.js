//operadores aritmeticos

/* Os operadores tem algumas formas de atribuicao. mas eles podem ser classificados encima de operandos */

const [ a, b, c, d ] = [3 , 5 , 1 , 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //resto da divisao
